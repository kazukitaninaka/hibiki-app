#seigaihaPug.mb-5
	h3
		strong.pl-2 青海波		
	.row.mt-3
		.col
			h5
				u 前
			//- - for (let i = 0; i < 6; i++)
			select.selectpicker.seigaiha_front.w-100.mt-2.border.border-secondary(name="seigaiha_front" multiple required data-live-search="true" data-max-options="6" title="選択してください" data-size="10")
				each member in members
					option(value=`${member.name}`) #{member.name}