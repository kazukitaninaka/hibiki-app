#openingPug.mb-5
	h3
		strong.pl-2 オープニング
	.row
		.col
			h5
				u 前
			//- - for (let i = 0; i < 6; i++)
			select.selectpicker.opening_front.w-100.mt-2.border.border-secondary(name="opening_front" multiple required data-live-search="true" data-max-options="6" title="選択してください" data-size="10")
				each member in members
					option(value=`${member.name}`) #{member.name}
	.row
		.col
			.row.mt-3
				.col
					h5
						u カラ
					select.selectpicker.w-100.mt-2.border.border-secondary(name="opening_kara" title="選択してください" data-live-search="true" required data-size="10")
						//- option(value="") 選択してください
						each member in members
							option #{member.name}
		.col
			.row.mt-3
				.col
					h5
						u おろし
					select.selectpicker.w-100.mt-2.border.border-secondary(name="opening_start" title="選択してください" data-live-search="true" required data-size="10")
						//- option(value="") 選択してください
						each member in bigMembers
							option #{member.name}
					